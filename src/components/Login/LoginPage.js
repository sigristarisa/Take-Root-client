import SignupHeader from "../Signup/SignupHeader";
import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <div>
      <SignupHeader />
      <LoginForm />
    </div>
  );
};

export default LoginPage;
